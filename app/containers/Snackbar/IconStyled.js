import Grid from 'material-ui/Grid';
import styled from 'styled-components';

// Style of Icon for snack bar
const IconStyled = styled(Grid)`
  color: ${props => props.color};
  float: left;
`;

export default IconStyled;
